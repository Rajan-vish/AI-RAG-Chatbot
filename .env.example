# Gemini API Key (required)
# Get your API key from: https://makersuite.google.com/app/apikey or gemini ai studio
GOOGLE_API_KEY=your_api_key_here

# Backend API URL (optional, defaults to http://localhost:8000)
BACKEND_URL=http://localhost:8000

# Chroma DB persistence directory (optional, defaults to ./chroma_data)
CHROMA_PERSIST_DIR=./chroma_data

# Embedding model path (optional, defaults to .models/nomic-embed-text-v1)
# For custom location or air-gapped setup, specify path to nomic-embed-text-v1
EMBEDDING_MODEL_PATH=./models/nomic-embed-text-v1

# Embedding Provider Configuration
# Options:
# - local (default): Use local nomic-embed-text-v1 model (requires model download)
# - gemini: Use Google Gemini text-embedding-004 (requires GOOGLE_API_KEY)
EMBEDDING_PROVIDER=local

# ============================================================================
# Flask UI HTTPS Configuration (for production or remote access)
# ============================================================================

# Enable HTTPS for Flask UI (required for voice features on non-localhost)
# Set to "true" to enable HTTPS, "false" for HTTP only (default: false)
HTTPS_ENABLED=false

# Copy the lines below if you want to enable HTTPS. Else only copy the above lines in .env

# Below are the paths to your SSL certificate and key files (optional, only use if HTTPS_ENABLED=true)
# If not provided with HTTPS_ENABLED=true, Flask will use adhoc self-signed cert

# SSL Certificate paths (optional, only needed if HTTPS_ENABLED=true)
# If not provided with HTTPS_ENABLED=true, Flask will use adhoc self-signed cert
# For production, use proper certificates from Let's Encrypt or your CA
SSL_CERT_PATH=/path/to/cert.pem
SSL_KEY_PATH=/path/to/key.pem

# ============================================================================
# Backend CORS Security
# ============================================================================

# Allowed origins for CORS (comma-separated list)
# Default allows common development origins
# For production, specify your actual frontend URL(s)
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000,https://yourdomain.com

